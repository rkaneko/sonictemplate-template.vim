'use strict';

const test = require('ava');

test.before(t => {
});

test.beforeEach(t => {
});

test.afterEach(t => {
});

test.after(t => {
});

test('{{_input_:specdesc}}', t => {
  // setup
  {{_cursor_}}  
  // exec

  // verify
  t.truthy(true);
});
